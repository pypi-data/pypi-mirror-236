from setuptools import setup

setup(name='Salman_distributions',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['Salman_distributions'],
      author=['Salman Gassem'],
      zip_safe=False)
