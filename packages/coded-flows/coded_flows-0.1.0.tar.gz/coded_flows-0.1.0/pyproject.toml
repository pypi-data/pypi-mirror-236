[tool.poetry]
name = "coded-flows"
version = "0.1.0"
description = "Code utilities for Coded Flows"
authors = ["COLOR CODED CODES <contact@colorcoded.codes>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
pydantic = "^2.4.2"
python-magic = "^0.4.27"
pytest = "^7.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
