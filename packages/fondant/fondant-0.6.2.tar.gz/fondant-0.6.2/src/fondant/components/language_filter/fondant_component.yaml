name: Filter languages
description: A component that filters text based on the provided language.
image: fndnt/language_filter:0.6.2

consumes:
  text:
    fields:
      data:
        type: string

args:
  language:
    description: A valid language code or identifier (e.g., "en", "fr", "de").
    type: str
    default: "en"
