[project]
name = "hello_aaingyunii"
version = "0.2.0"
description = "The python program I'm distributing for the first time"
authors = [
    { name = "aaingyunii", email = "dlsrbs98@gmail.com" },
]
dependencies = []
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
