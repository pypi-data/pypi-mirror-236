from setuptools import setup, find_packages

setup(
    name='jaxlob',
    version='0.10',
    packages=find_packages(),
    install_requires=[
        # list your project dependencies here
    ],
)

