[project]
name = "jiug_hello"
version = "0.3.1"
description = "The Python program I'm distributing for the first time"
authors = [
    { name = "jiughan", email = "wldnd4081@gmail.com" },
]
dependencies = []
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
jiug_hello-pic = "jiug_hello.my:pic"

[project.urls]
Homepage = "https://github.com/Sam1000won/jiug"
"Bug Tracker" = "https://github.com/Sam1000won/jiug/issues"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.dev-dependencies]
test = [
    "pytest>=7.4.2",
    "pytest-cov>=4.1.0",
]
