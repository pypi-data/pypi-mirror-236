[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lossy-mpi"
version = "0.1.1"
description = "MPI Pool communicator that is toleration to rank failures"
dependencies = ["pytest-mpi ==0.6"]

[project.optional-dependencies]
dev = [
    "black ==23.3.0",
    "pytest ==7.3.1",
    "pytest-mpi ==0.6",
    "ruff ==0.0.290",
    "twine ==4.0.2",
    "build ==1.0.3",
]
