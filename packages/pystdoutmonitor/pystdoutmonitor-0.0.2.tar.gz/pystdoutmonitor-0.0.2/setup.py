from setuptools import setup

setup(name='pystdoutmonitor',
      version='0.0.2',
      description='Launches a distinct thread and calls a user-defined callback each time it captures an output.',
      packages=['stdoutmonitor'],
      author_email='sevaserg@gmail.com',
      zip_safe=False)
