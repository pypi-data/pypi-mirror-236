from setuptools import setup, find_packages

setup(
    name='quiktools',
    version='0.1',
    description="Quickly do things in 1 line that would've taken multiple lines.",
    author='noerlol',
    author_email='noerlol@proton.com',
    packages=['quiktools'],
    install_requires=[],
)
